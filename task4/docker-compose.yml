services:
  back-end:
    build: ./back-end
    ports:
      - "5252:5252"
    networks:
      - app-network

  front-end:
    build: ./front-end
    ports:
      - "9000:9000"
    depends_on:
      - back-end
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
